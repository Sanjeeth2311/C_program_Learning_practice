Get a two-digit number from the user and check if the digit 0 is greater than or equal to the digit 1. if yes, the print 1, otherwise print 0.

_____________________________________________

#include <stdio.h>

int main() {
    int a,b,c,d;
    scanf("%d",&a);
   b = a%10;
   c=a/10;
   d=(b<=c);
   
    printf("%d",d);
    return 0;
}
