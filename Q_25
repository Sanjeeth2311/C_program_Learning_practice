Get a number from the user and check if the number is not equals to 50. if yes, the print 1, otherwise print 0.

_____________________________________

#include <stdio.h>

int main() {
    int a,b,c,d;
    scanf("%d",&a);
    b = (a!=50);
   
    printf("%d",b);
    return 0;
}
