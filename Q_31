Get a two-digits number from the user and check if the digit 0 and 1 are not identical. if yes, the print 1, otherwise print 0.

______________________________________

#include <stdio.h>

int main() {
    int a,b,c,d;
    scanf("%d",&a);
   b = a%10;
   c=a/10;
   d=(b!=c);
   
    printf("%d",d);
    return 0;
}
