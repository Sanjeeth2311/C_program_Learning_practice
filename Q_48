Get a two-digit number from the user and chech if the digit 0 is less than or equal to the digit 1 are identical. If yes, the print "Success", otherwise print "Failure".

____________________________________________

#include <stdio.h>

int main() {
    int a,b,c,d;
    scanf("%d",&a);
    b=a%10;
    c=a/10;
    if(b<=c){ 
        printf("Success");
    }
    else{
        printf("Failure");
    }
    
    return 0;
}
