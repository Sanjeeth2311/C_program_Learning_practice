Get a number from the user and check if the number is greater than or equal to 50. if yes, the print 1, otherwise print 0.

___________________________________

#include <stdio.h>

int main() {
    int a,b,c,d;
    scanf("%d",&a);
    b = (a>= 50);
   
    printf("%d",b);
    return 0;
}
